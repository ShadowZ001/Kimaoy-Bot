const { guidePages, getGuideComponents } = require('../../utils/guideData');module.exports = {    customId: 'eb_guide_next',    async execute(interaction) {        const parts = interaction.customId.split('_');        const currentPage = parseInt(parts[3], 10);        const newPage = currentPage + 1;        if (newPage < guidePages.length) {            await interaction.update({                embeds: [guidePages[newPage]],                components: [getGuideComponents(newPage)]            });        } else {            await interaction.deferUpdate();        }    }};