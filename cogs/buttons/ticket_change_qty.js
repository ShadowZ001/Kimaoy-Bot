const { ModalBuilder, TextInputBuilder, TextInputStyle, ActionRowBuilder } = require('discord.js');module.exports = {    customId: 'ticket_change_qty',    async execute(interaction, client) {        const modal = new ModalBuilder()            .setCustomId('modal_change_qty')            .setTitle('Change Order Quantity');        const qtyInput = new TextInputBuilder()            .setCustomId('qtyInputId')            .setLabel('New Quantity')            .setPlaceholder('Enter the new quantity (e.g., 5x or Custom)')            .setStyle(TextInputStyle.Short)            .setRequired(true)            .setMaxLength(20);        const actionRow = new ActionRowBuilder().addComponents(qtyInput);        modal.addComponents(actionRow);        await interaction.showModal(modal);    }};