module.exports = {    customId: 'ticket_mm',    async execute(interaction, client) {
        await interaction.deferReply({ ephemeral: true });        const mmRoleId = process.env.MM_ROLE_ID || '000000000000000000';        const pingText = mmRoleId !== '000000000000000000' ? `<@&${mmRoleId}>` : '@here';        const cv2Payload = {            flags: (1 << 15),            components: [{                type: 17,                accent_color: 3793022,                 components: [{                    type: 10,                    content: `${pingText}\n> <:0029_3:1534543199421468692> | **Middleman Requested**\n> A middleman has been requested by <@${interaction.user.id}>. Please wait for an available MM.`                }]            }]        };        await interaction.editReply({ flags: cv2Payload.flags, components: cv2Payload.components });    }};